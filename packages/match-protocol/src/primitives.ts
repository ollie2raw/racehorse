/** Local two-player match seat identifiers (Play vs Fritz, Daily Fritz embed, ghost). */
export type LocalPlayerId = 'you' | 'bot';

export type BotHandEndReason = 'domino' | 'blocked';

export type BotDealSize = 7 | 14;